# Claude Instructions — GMM Budget App

## Deployment

- **Always deploy via GitHub → Vercel.** Never use `localhost` or `vercel deploy` without pushing first.
- **Before every commit, run `git status`** and stage ALL modified files. Never commit a partial set of changes — untracked edits will revert on the next push.
- Push to `main` to trigger an automatic Vercel deployment.
